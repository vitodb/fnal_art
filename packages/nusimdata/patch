diff --git a/CMakeLists.txt b/CMakeLists.txt
index 251ea92..63569ad 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -19,17 +19,7 @@ cmake_minimum_required (VERSION 3.3)
 
 project(nusimdata)
 
-# cetbuildtools contains our cmake modules
-SET ( CETBUILDTOOLS_VERSION $ENV{CETBUILDTOOLS_VERSION} )
-IF (NOT CETBUILDTOOLS_VERSION)
-    MESSAGE (FATAL_ERROR "ERROR: setup cetbuildtools to get the cmake modules")
-ENDIF()
-
-set(CMAKE_MODULE_PATH $ENV{CANVAS_ROOT_IO_DIR}/Modules
-                      $ENV{CETBUILDTOOLS_DIR}/Modules
-                      ${CMAKE_MODULE_PATH})
-
-
+find_package(cetmodules REQUIRED)
 include(CetCMakeEnv)
 cet_cmake_env()
 
@@ -42,10 +32,13 @@ cet_set_compiler_flags(DIAGS CAUTIOUS WERROR
 cet_report_compiler_flags()
 
 # these are minimum required versions, not the actual product versions
-find_ups_product( canvas )
-find_ups_product( dk2nudata v01_05_01 )
-find_ups_product( cetbuildtools )
-find_ups_root(  )
+find_package( Boost COMPONENTS date_time filesystem program_options
+              regex system unit_test_framework REQUIRED)
+find_package( canvas REQUIRED )
+find_package( canvas_root_io REQUIRED )
+find_package( cetlib REQUIRED )
+find_package( cetlib_except REQUIRED )
+find_package( ROOT  REQUIRED  COMPONENTS)
 
 # macros 
 include(CetMake)
@@ -54,9 +47,3 @@ include(BasicPlugin)
 
 # source
 add_subdirectory (nusimdata)
-add_subdirectory (test)
-
-# ups - table and config files
-add_subdirectory(ups)
-# packaging utility
-include(UseCPack)
diff --git a/nusimdata/SimulationBase/CMakeLists.txt b/nusimdata/SimulationBase/CMakeLists.txt
index 63d49ac..b10341f 100644
--- a/nusimdata/SimulationBase/CMakeLists.txt
+++ b/nusimdata/SimulationBase/CMakeLists.txt
@@ -1,34 +1,16 @@
 
-cet_find_library( DK2NU_TREE NAMES dk2nuTree PATHS ENV DK2NUDATA_LIB NO_DEFAULT_PATH )
+find_library( DK2NU_TREE NAMES dk2nuTree PATHS ENV DK2NUDATA_LIB)
 include_directories( $ENV{DK2NUDATA_INC} )
-
-# ROOT_BASIC_LIB_LIST is:
-#   Core RIO Net Imt Hist Graf Graf3d Gpad Tree Rint Postscript
-#   Matrix Physics MathCore Thread
+include_directories( ${Boost_INCLUDE_DIR} )
 
 set( SIMB_LIBS
                canvas
                fhiclcpp
-               ${DK2NU_TREE}  # additional data objects assoc w/ MCTruth
+               ${DK2NU_TREE} # additional data objects assoc w/ MCTruth
                cetlib
                cetlib_except
-               ${ROOT_BASIC_LIB_LIST}
-               ${ROOT_EG} )  # EG needed for TParticleDB
-#               ${ROOT_CORE}
-#               ${ROOT_CINT}
-#               ${ROOT_RIO}
-#               ${ROOT_NET}
-#               ${ROOT_HIST}
-#               ${ROOT_GRAF}
-#               ${ROOT_GRAF3D}
-#               ${ROOT_GPAD}
-#               ${ROOT_TREE}
-#               ${ROOT_RINT}
-#               ${ROOT_POSTSCRIPT}
-#               ${ROOT_MATRIX}
-#               ${ROOT_PHYSICS}
-#               ${ROOT_MATHCORE}
-#               ${ROOT_THREAD}
+               ROOT::EG   # EG needed for TParticleDB
+               ${ROOT_BASIC_LIB_LIST} )
 
 cet_make( LIBRARIES ${SIMB_LIBS}
           NO_DICTIONARY )
@@ -36,5 +18,3 @@ cet_make( LIBRARIES ${SIMB_LIBS}
 art_dictionary( DICTIONARY_LIBRARIES nusimdata_SimulationBase )
 
 install_headers()
-install_fhicl()
-install_source()
