diff --git a/CMakeLists.txt b/CMakeLists.txt
index 3ee6e54..c4ed98e 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -13,21 +13,14 @@
 # ======================================================================
 
 
-# use cmake 2.8 or later
-cmake_minimum_required (VERSION 2.8)
+# use cmake 3.11 or later
+cmake_minimum_required (VERSION 3.11)
 
-project(larevt)
-
-# cetbuildtools contains our cmake modules
-SET ( CETBUILDTOOLS_VERSION $ENV{CETBUILDTOOLS_VERSION} )
-IF (NOT CETBUILDTOOLS_VERSION)
-    MESSAGE (FATAL_ERROR "ERROR: setup cetbuildtools to get the cmake modules")
-ENDIF()
-
-set(CMAKE_MODULE_PATH $ENV{ART_DIR}/Modules
-		      $ENV{CETBUILDTOOLS_DIR}/Modules
-		      ${CMAKE_MODULE_PATH})
+project(larevt VERSION 8.02.04 LANGUAGES CXX)
 
+set(fcl_dir job)
+set(gdml_dir gdml)
+find_package( cetmodules REQUIRED )
 include(CetCMakeEnv)
 cet_cmake_env()
 
@@ -40,19 +33,9 @@ cet_set_compiler_flags(DIAGS CAUTIOUS
 cet_report_compiler_flags()
 
 # these are minimum required versions, not the actual product versions
-find_ups_product( larcore v1_00_00 )
-find_ups_product( lardata v1_00_00 )
-find_ups_product( nutools v1_00_00 )
-find_ups_product( art v1_09_00 )
-find_ups_product( messagefacility v1_10_00 )
-find_ups_product( cetbuildtools v4_09_00 )
-find_ups_root( root v5_34_00 )
-find_ups_product( postgresql v9_1_5 )
-find_ups_product( libwda v2_21_0 )
-
-# include search path
-# notice use of ENV to invoke an external variable
-include_directories ( $ENV{NUTOOLS_INC} )
+find_package( PostgreSQL REQUIRED )
+find_package( ROOT REQUIRED COMPONENTS FFTW )
+find_package( lardata REQUIRED )
 
 # macros for dictionary and simple_plugin
 include(ArtDictionary)
@@ -60,15 +43,13 @@ include(ArtMake)
 include(BuildPlugins)
 
 # source
-add_subdirectory(CalData)
-add_subdirectory(Filters)
-add_subdirectory(CalibrationDBI)
-add_subdirectory(SpaceCharge)
-# tests
-# add_subdirectory(test)
+add_subdirectory(larevt)
 
+# tests
+add_subdirectory(test)
+cet_cmake_config()
 # ups - table and config files
-add_subdirectory(ups)
+#add_subdirectory(ups)
 
 # packaging utility
-include(UseCPack)
+#include(UseCPack)
