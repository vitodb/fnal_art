diff --git a/CMakeLists.txt b/CMakeLists.txt
index f6623d2..94ad179 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -13,21 +13,14 @@
 # ======================================================================
 
 
-# use cmake 2.8 or later
-cmake_minimum_required (VERSION 2.8)
+# use cmake 3.11 or later
+cmake_minimum_required (VERSION 3.11)
 
-project(larcore)
-
-# cetbuildtools contains our cmake modules
-SET ( CETBUILDTOOLS_VERSION $ENV{CETBUILDTOOLS_VERSION} )
-IF (NOT CETBUILDTOOLS_VERSION)
-    MESSAGE (FATAL_ERROR "ERROR: setup cetbuildtools to get the cmake modules")
-ENDIF()
-
-set(CMAKE_MODULE_PATH $ENV{ART_DIR}/Modules
-		      $ENV{CETBUILDTOOLS_DIR}/Modules
-		      ${CMAKE_MODULE_PATH})
+project(larcore VERSION 08.02.03 LANGUAGES CXX)
 
+set(fcl_dir job)
+set(gdml_dir gdml)
+find_package( cetmodules )
 include(CetCMakeEnv)
 cet_cmake_env()
 
@@ -40,10 +33,11 @@ cet_set_compiler_flags(DIAGS CAUTIOUS
 cet_report_compiler_flags()
 
 # these are minimum required versions, not the actual product versions
-find_ups_product( art v1_09_00 )
-find_ups_product( messagefacility v1_10_00 )
-find_ups_product( cetbuildtools v4_09_00 )
-find_ups_root( root v5_34_00 )
+find_package( ROOT REQUIRED COMPONENTS Core Geom GenVector)
+find_package(Boost REQUIRED COMPONENTS date_time filesystem graph program_options
+  regex system unit_test_framework)
+find_package( larcorealg REQUIRED)
+find_package( art REQUIRED)
 
 # macros for artdaq_dictionary and simple_plugin
 include(ArtDictionary)
@@ -51,15 +45,14 @@ include(ArtMake)
 include(BuildPlugins)
 
 # source
-add_subdirectory(Geometry)
-add_subdirectory(SimpleTypesAndConstants)
-add_subdirectory(SummaryData)
+add_subdirectory(larcore)
 
 # tests 
 add_subdirectory(test)
-
+add_subdirectory(cmakeConfig)
 # ups - table and config files
-add_subdirectory(ups)
+#add_subdirectory(ups)
 
 # packaging utility
-include(UseCPack)
+#include(UseCPack)
+cet_cmake_config()
