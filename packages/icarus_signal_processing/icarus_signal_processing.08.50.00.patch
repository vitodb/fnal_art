
diff --git a/CMakeLists.txt b/CMakeLists.txt
index df41978..be44290 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -16,20 +16,17 @@
 # use cmake 3.11 or later
 cmake_minimum_required (VERSION 3.11)
 
-project(icarus_signal_processing)
+project(icarus_signal_processing VERSION 08.50.00 LANGUAGES CXX)
 
 # cetbuildtools contains our cmake modules
-find_package(cetbuildtools REQUIRED)
-
-list(APPEND CMAKE_MODULE_PATH
-     $ENV{CANVAS_ROOT_IO_DIR}/Modules
-     $ENV{ART_DIR}/Modules
-#     ${CMAKE_CURRENT_SOURCE_DIR}/Modules
-)
+find_package(cetmodules  REQUIRED)
 
 include(CetCMakeEnv)
 cet_cmake_env()
 
+set(fcl_dir 'job')
+set(gdml_dir 'gdml')
+
 cet_set_compiler_flags(DIAGS CAUTIOUS
   WERROR
   NO_UNDEFINED
@@ -40,10 +37,13 @@ cet_set_compiler_flags(DIAGS CAUTIOUS
 cet_report_compiler_flags()
 
 # these are minimum required versions, not the actual product versions
-find_ups_product( larsoft )
-find_ups_product( nutools )
-find_ups_product( art )
-find_ups_product( cetbuildtools ) # LIBRARY_OUTPUT_DIRECTORY, etc.
+find_package( larsoft )
+find_package( canvas_root_io )
+find_package( nutools )
+find_package( art )
+find_package( Eigen3 )
+include_directories( ${EIGEN3_INCLUDE_DIRS} )
+set(CMAKE_CXX_FLAGS "${CMAKE_CXX_FLAGS} -Wno-shadow")
 
 # macros for dictionary and simple_plugin
 include(ArtDictionary)
@@ -53,21 +50,17 @@ include(BuildPlugins)
 # add cet_find_library commands here when needed
 
 ##cet_find_library(FFTW_LIBRARY NAMES fftw3f fftw3 fftw3q fftw3l fftw3_threads fftw3f_threads fftw3q_threads PATHS $ENV{FFTW_LIBRARY} )
-cet_find_library(ICARUS_FFTW3_LIBRARY  NAMES fftw3  PATHS $ENV{FFTW_LIBRARY} )
-cet_find_library(ICARUS_FFTW3F_LIBRARY NAMES fftw3f PATHS $ENV{FFTW_LIBRARY} )
-cet_find_library(ICARUS_FFTW3L_LIBRARY NAMES fftw3l PATHS $ENV{FFTW_LIBRARY} )
-cet_find_library(ICARUS_FFTW3Q_LIBRARY NAMES fftw3q PATHS $ENV{FFTW_LIBRARY} )
+cet_find_library(ICARUS_FFTW3_LIBRARY  NAMES fftw3   )
+cet_find_library(ICARUS_FFTW3F_LIBRARY NAMES fftw3f  )
+cet_find_library(ICARUS_FFTW3L_LIBRARY NAMES fftw3l  )
+cet_find_library(ICARUS_FFTW3Q_LIBRARY NAMES fftw3q  )
+get_filename_component(ICARUS_LIBDIR ${ICARUS_FFTW3_LIBRARY} DIRECTORY)
+include_directories("${ICARUS_LIBDIR}/../include")
 set(ICARUS_FFTW_LIBRARIES ${ICARUS_FFTW3_LIBRARY} ${ICARUS_FFTW3F_LIBRARY} ${ICARUS_FFTW3L_LIBRARY} ${ICARUS_FFTW3Q_LIBRARY})
-
 # ADD SOURCE CODE SUBDIRECTORIES HERE
 add_subdirectory(icarus_signal_processing)
 
 # tests
 add_subdirectory(test)
 
-# ups - table and config files
-# must be AFTER all other subdirectories
-add_subdirectory(ups)
-
-# packaging utility - must be last
-include(UseCPack)
+cet_cmake_config()
diff --git a/icarus_signal_processing/CMakeLists.txt b/icarus_signal_processing/CMakeLists.txt
index 490e7ef..4048ea5 100755
--- a/icarus_signal_processing/CMakeLists.txt
+++ b/icarus_signal_processing/CMakeLists.txt
@@ -3,11 +3,11 @@ add_definitions(-DEIGEN_FFTW_DEFAULT)
 art_make( 
           LIB_LIBRARIES 
                           ${ICARUS_FFTW_LIBRARIES}
-                          ${ROOT_GEOM}
-                          ${ROOT_XMLIO}
-                          ${ROOT_GDML}
-                          ${ROOT_FFTW}
-                          ${ROOT_BASIC_LIB_LIST}
+			  ROOT::Geom
+			  ROOT::XMLIO
+			  ROOT::Gdml
+			  ROOT::FFTW
+			  ${ROOT_LIBRARIES}
        )
 
 install_headers()
