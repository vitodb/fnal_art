diff --git a/CMakeLists.txt b/CMakeLists.txt
index aca6df3..3f5e8be 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -18,16 +18,7 @@ cmake_minimum_required (VERSION 3.3)
 
 project(lardataalg)
 
-# cetbuildtools contains our cmake modules
-SET ( CETBUILDTOOLS_VERSION $ENV{CETBUILDTOOLS_VERSION} )
-IF (NOT CETBUILDTOOLS_VERSION)
-    MESSAGE (FATAL_ERROR "ERROR: setup cetbuildtools to get the cmake modules")
-ENDIF()
-
-set(CMAKE_MODULE_PATH $ENV{CANVAS_ROOT_IO_DIR}/Modules
-		      $ENV{CETBUILDTOOLS_DIR}/Modules
-		      ${CMAKE_MODULE_PATH})
-
+find_package( cetmodules )
 include(CetCMakeEnv)
 cet_cmake_env()
 
@@ -41,12 +32,16 @@ cet_set_compiler_flags(DIAGS CAUTIOUS
 cet_report_compiler_flags()
 
 # these are minimum required versions, not the actual product versions
-find_ups_product( canvas )
-find_ups_product( cetbuildtools )
-find_ups_product( larcoreobj )
-find_ups_product( larcorealg )
-find_ups_product( nusimdata )
-find_ups_root( )
+find_package( canvas REQUIRED)
+find_package( canvas_root_io REQUIRED)
+find_package( cetlib REQUIRED)
+find_package( cetlib_except REQUIRED)
+find_package( messagefacility REQUIRED)
+find_package( fhiclcpp REQUIRED)
+find_package( larcoreobj )
+find_package( larcorealg )
+find_package( nusimdata )
+find_package( ROOT REQUIRED COMPONENTS)
 
 # macros for artdaq_dictionary and simple_plugin
 include(ArtDictionary)
@@ -57,10 +52,10 @@ include(BasicPlugin)
 add_subdirectory(lardataalg)
 
 # tests 
-add_subdirectory(test)
+#add_subdirectory(test)
 
 # ups - table and config files
-add_subdirectory(ups)
+#add_subdirectory(ups)
 
 # packaging utility
-include(UseCPack)
+#include(UseCPack)
diff --git a/lardataalg/DetectorInfo/CMakeLists.txt b/lardataalg/DetectorInfo/CMakeLists.txt
index 469bcea..70548ba 100644
--- a/lardataalg/DetectorInfo/CMakeLists.txt
+++ b/lardataalg/DetectorInfo/CMakeLists.txt
@@ -6,8 +6,8 @@ cet_make( LIBRARIES
                     cetlib
 		    cetlib_except
 		    ${FHICLCPP}
-                    ${ROOT_BASIC_LIB_LIST}
-		    ${Boost_SYSTEM_LIBRARY}
+                    ${ROOT_LIBRARIES}
+		    ${Boost_System_LIBRARY}
         )
 
 
diff --git a/lardataalg/MCDumpers/CMakeLists.txt b/lardataalg/MCDumpers/CMakeLists.txt
index b01e621..ee91f53 100644
--- a/lardataalg/MCDumpers/CMakeLists.txt
+++ b/lardataalg/MCDumpers/CMakeLists.txt
@@ -1,6 +1,6 @@
 cet_make( LIBRARIES
-    ${ROOT_EG}
-    ${ROOT_CORE}
+    ROOT::EG
+    ROOT::Core
   )
 
 install_headers()
diff --git a/ups/CMakeLists.txt b/ups/CMakeLists.txt
index fe33cc7..fc562d5 100644
--- a/ups/CMakeLists.txt
+++ b/ups/CMakeLists.txt
@@ -1,6 +1,6 @@
 
 # create package configuration and version files
 
-process_ups_files()
+#process_ups_files()
 
 cet_cmake_config()
